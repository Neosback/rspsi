package com.rspsi.cache;

/** Neutral asset families exposed by a selected cache bundle. */
public enum AssetCategory {
    OBJECTS,
    ITEMS,
    NPCS,
    UNDERLAYS,
    OVERLAYS,
    TEXTURES,
    MODELS,
    SPRITES,
    MAP_SCENES,
    SEQUENCES,
    MAP_ELEMENTS,
    INTERFACES,
    CLIENTSCRIPTS,
    GAMEVALS,
    DB_TABLES
}
